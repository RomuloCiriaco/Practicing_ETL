# Inventário de máquinas (genérico)

Consolida scans JSON em `dados/inventario.csv`.

## Uso

```bash
cd exercicios/inventario_maquinas
# coloque JSONs em dados/scans/ (ou use a pasta do pendrive)
python3 consolidar_inventario.py
```

Dados reais ficam **fora do Git**. No pendrive de trabalho:

`/media/romulo/KINGSTON/SATO-INVENTARIO/`

lá estão `consolidar_inventario.py`, `executar.bat`, scanner `.exe` e os CSVs.
